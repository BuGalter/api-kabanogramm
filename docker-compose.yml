version: '3.9'

services:
  mysql:
    image: mysql:latest
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: kabanogramm
      MYSQL_USER: kaban
      MYSQL_PASSWORD: kaban
    ports:
      - '3306:3306'