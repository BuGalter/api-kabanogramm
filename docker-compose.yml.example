version: '3.9'

services:
  mysql:
    image: mysql:latest
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root_passsword
      MYSQL_DATABASE: name_database
      MYSQL_USER: user_name
      MYSQL_PASSWORD: user_password
    ports:
      - '3306:3306'