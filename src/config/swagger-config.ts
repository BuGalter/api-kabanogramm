import { SwaggerConfig } from './interfaces/swagger-config.interface';

export default (): SwaggerConfig => ({
  title: 'API Kabanogramm',
  description: 'The API messenger Kabanogramm description',
  version: '1.0',
});
